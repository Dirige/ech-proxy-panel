# ECH Proxy Panel 修复文档

本文档记录本次修复的所有问题、根因和改动，供重新部署/二次开发参考。

---

## 一、问题清单与根因

### 1. 分流错乱：国外 IP 被当成中国 IP 走直连

**现象**：Telegram（`91.108.56.130`）、Cloudflare（`1.1.1.1`）等国外 IP 在 `bypass_cn` 模式下被判为直连，随后连接超时。

**根因**：程序运行时从 GitHub 下载 `chn_ip.txt`，但下载过程被网络中间设备**篡改**。实测下载到的内容第一行是：

```
1.0.1.0 255.1.0.1     ← 错误（结束 IP 被颠倒）
```

正确应为：

```
1.0.1.0 1.0.1.255     ← 正确
```

被篡改后，解析出的区间会覆盖几乎所有 IP，导致所有 IP 都被判定为“中国 IP”。

**修复**：
- **构建镜像时下载 IP 列表并打包进镜像**（GitHub Actions 网络干净，不会被篡改），运行时优先使用内置列表，无需联网下载。
- 加载列表后做**完整性校验**：用已知国外 IP（`8.8.8.8`、`1.1.1.1`、`91.108.56.130`、`172.67.74.152`、`104.16.0.1`）反查，只要有任意一个被判为中国 IP，就判定列表损坏，丢弃并回退为“全局代理”（全部走代理，安全兜底）。
- 解析支持 `start end` 和 `CIDR` 两种格式，并**合并重叠/相邻区间**，保证二分查找正确。

### 2. 自定义代理规则不生效

**现象**：在面板“规则”页把域名加入代理列表，但该域名仍然走直连；即使切到全局模式也一样。

**根因**：`shouldBypassProxy()` 只在 `routingMode == "custom"` 时才检查自定义规则。在 `global` / `bypass_cn` / `none` 模式下，面板规则被完全忽略。

**修复**：自定义规则**在所有分流模式下都优先匹配**：
- 命中 `direct` 规则 → 直连
- 命中 `proxy` 规则 → 走代理
- 未命中 → 再按分流模式决定

域名规则支持精确匹配、子域名匹配（`255432.xyz` 匹配 `emos.255432.xyz`）和通配符（`*.255432.xyz`）。

### 3. 配置页所有参数灰色不可编辑

**根因**：前端 `index.html` 中部分输入框写死了 `readonly`，且 `saveConfig()` 只提交了 4 个字段，后端 `handleConfig` 也只接收 4 个字段。

**修复**：
- 移除所有 `readonly`。
- 前端提交全部字段（监听地址、服务端地址、优选 IP、令牌、DoH、ECH 域名、分流模式、固定出口 IP、面板地址）。
- 后端接收并保存全部字段，写回 `config.json`，重启后生效。

### 4. 播放视频一会就断开

**根因**：WebSocket 长连接没有写超时，客户端连接也没有读超时。当 Worker 端断开或网络抖动时，写操作永久阻塞，连接卡死。

**修复**：
- WebSocket 读超时 60s → 120s（视频可能长时间无数据）。
- 写入数据加 30s 写超时，发送 `CLOSE` 加 10s 写超时，Ping 加 15s 写超时。
- Ping 间隔 15s → 20s。
- 双向转发的 `done` 通道改为非阻塞通知，避免两个 goroutine 同时写入阻塞。

### 5. 构建/其他已知问题（前序修复）

- Dockerfile 去掉 `go.mod/go.sum` 的 COPY，改为 `go mod init + tidy` 自包含构建。
- 移除仓库中的真实 `config.json`，改用 `config.example.json` 模板并加入 `.gitignore`。
- 实时速度由服务端计算（`rt_upload`/`rt_download`，单位 B/s），前端直接展示。
- 修复数据竞争：`closed` 改 `atomic.Bool`，`handleStatus` 读 `customRules` 加锁。
- 连接数用 `atomic` 计数器，避免全量遍历 `sync.Map`。

---

## 二、本次改动的文件

| 文件 | 改动 |
|------|------|
| `ech-workers.go` | 分流逻辑重写；自定义规则全模式生效；IP 列表解析/校验/合并；路由日志加模式信息 |
| `Dockerfile` | 构建阶段下载并打包 `chn_ip.txt` / `chn_ip_v6.txt` |
| `index.html` | 配置页去掉 `readonly`；保存全部字段 |

### 关键代码位置

- `shouldBypassProxy()` / `matchCustomRule()`：分流决策
- `loadChinaIPList()` / `mergeIPv4Ranges()` / `findIPv4Range()` / `lastIP()`：IP 列表加载与匹配
- `handleTunnel()` / `handleDirectConnection()`：连接转发与超时
- `handleConfig()`：配置读写

---

## 三、分流模式说明

| 模式 | 行为 |
|------|------|
| `global` | 全部走代理 |
| `bypass_cn` | 中国 IP 直连，其余走代理（依赖内置 IP 列表） |
| `none` | 全部直连（不改变代理） |
| `custom` | 按自定义规则；未命中默认走代理 |

> 自定义规则**在任何模式下都优先**，可用来强制某个域名/网段直连或走代理。

---

## 四、重新部署步骤

### 1. 推送代码触发构建

推送到 `main` 分支后，GitHub Actions（`.github/workflows/docker.yml`）自动构建镜像并推送到：

```
ghcr.io/dirige/ech-proxy-panel:latest
```

### 2. iKuai 创建容器（UI 方式，推荐）

1. 高级应用 → 插件管理 → docker → 容器列表 → 添加
2. 镜像：`ghcr.io/dirige/ech-proxy-panel:latest`
3. 网络：桥接
4. 端口：`30000:30000`（代理）、`30001:9090`（面板）
5. 挂载目录（可选，用于持久化配置）：
   - 源路径：爱快“磁盘管理→文件管理”里普通存储的目录（必须是 Docker 存储分区内的路径）
   - 目标路径：`/data`
6. 高级选项 → command 填：
   ```
   -l 0.0.0.0:30000 -f hhech.nb1tap.kdns.fr:443 -token honghongfree -ip 172.64.229.240 -web :9090 -routing global
   ```
7. 保存启动

### 3. 首次启动说明

- 若未挂载 `/data`，程序会把配置写入容器内 `/data/config.json`（容器重建后丢失）。
- 之后可在面板“配置”页修改所有参数并保存，重启容器生效。
- `bypass_cn` 模式直接使用镜像内置的中国 IP 列表，**不再联网下载**。

### 4. 面板地址

```
http://爱快IP:30001
```

播放器设置 SOCKS5 代理：`爱快IP:30000`。

---

## 五、配置字段说明（config.json）

```json
{
  "listen_addr": "0.0.0.0:30000",
  "server_addr": "hhech.nb1tap.kdns.fr:443",
  "server_ip": "172.64.229.240",
  "token": "honghongfree",
  "dns_server": "dns.alidns.com/dns-query",
  "ech_domain": "cloudflare-ech.com",
  "routing_mode": "bypass_cn",
  "web_addr": ":9090",
  "proxy_ip": ""
}
```

| 字段 | 说明 |
|------|------|
| `listen_addr` | 代理监听地址（SOCKS5 + HTTP） |
| `server_addr` | ECH Worker 服务端地址 |
| `server_ip` | 服务端优选 IP（绕过 DNS） |
| `token` | 身份验证令牌 |
| `dns_server` | DoH 服务器 |
| `ech_domain` | ECH 查询域名 |
| `routing_mode` | `global` / `bypass_cn` / `none` / `custom` |
| `web_addr` | 面板监听地址 |
| `proxy_ip` | 固定出口 IP，留空自动分配 |

---

## 六、验证要点

启动日志应包含：

```
[启动] 分流模式: 跳过中国大陆，正在加载中国IP列表...
[启动] 已加载 N 个中国IPv4段, M 个中国IPv6段
```

若出现：

```
[警告] 加载中国IPv4列表失败: IP列表校验失败: 已知国外IP被误判为中国IP
```

说明 IP 列表损坏，程序已自动回退为全局代理（安全兜底），不影响正常使用。

分流日志（新增模式标识）：

```
[分流] x.x.x.x -> 91.108.56.130:443 (通过代理, 模式=bypass_cn)
[分流] x.x.x.x -> 223.5.5.5:443 (直连, 模式=bypass_cn)
```
